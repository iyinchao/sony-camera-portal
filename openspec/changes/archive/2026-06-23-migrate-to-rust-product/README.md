# migrate-to-rust-product

Make Rust the canonical implementation: archive Go + rust-poc to docs/prototype, restructure into a Cargo workspace under packages/, and add runtime camera connection management (connect/change-IP/reconnect from the web UI).
