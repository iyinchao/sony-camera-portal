//! sony-camera-portal CLI: parses flags, embeds the web bundle, starts the
//! localhost server. Filled in by the `migrate-to-rust-product` change.

fn main() {
    println!("sony-camera-portal: workspace skeleton — port in progress");
}
