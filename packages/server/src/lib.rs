//! Localhost HTTP server: serves the embedded React UI and proxies `/api` to the
//! camera, with runtime connection state (connect / change IP / reconnect).
//!
//! Filled in by the `migrate-to-rust-product` change.
