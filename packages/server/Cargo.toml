[package]
name = "server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Localhost HTTP server: serves the embedded UI and proxies /api to the camera"

[dependencies]
camera = { path = "../camera" }
tiny_http = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
